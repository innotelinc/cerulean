services:
  cerulean:
    build: .
    container_name: cerulean
    restart: unless-stopped
    env_file: .env
    ports:
      - "${CERULEAN_PORT:-3000}:${CERULEAN_PORT:-3000}"
    volumes:
      - ./data:/app/data

  # ── Bundled BIND + SSH (OPT-IN: BIND_MODE=local, docker compose --profile bind up -d) ──
  # Authoritative DNS + sshd so Cerulean manages records exactly like a remote
  # BIND box (nsupdate over SSH + TSIG, dig AXFR). BIND_SSH_HOST defaults to
  # "cerulean-bind" inside the compose network; port 53 is published so the
  # LAN can query the zones, and 2222 maps to sshd for host-side debugging.
  cerulean-bind:
    build: ./bind
    container_name: cerulean-bind
    profiles: ["bind"]
    restart: unless-stopped
    environment:
      BIND_ZONES: ${BIND_ZONES:-${CERULEAN_ZONE:-}}
      BIND_TSIG_NAME: ${BIND_TSIG_NAME:-cerulean}
      BIND_TSIG_SECRET: ${BIND_TSIG_SECRET:-}
      BIND_SSH_PASSWORD: ${BIND_SSH_PASSWORD:-}
      BIND_PORTAL_IPS: ${BIND_PORTAL_IPS:-any}
    ports:
      - "${BIND_UDP_PORT:-53}:53/udp"
      - "${BIND_TCP_PORT:-53}:53/tcp"
      - "2222:22"
    volumes:
      - ./data/bind/zones:/etc/bind/zones

  # ── Bundled nginx proxy manager (OPT-IN: NPM_MODE=local, docker compose --profile npm up -d) ──
  # Self-contained reverse proxy + wildcard SSL for the portal's subdomains.
  # NPM_API_URL defaults to "http://cerulean-npm:81" inside the compose
  # network; the host-side scripts (npm-proxy-hosts.py) keep using
  # NPM_API_URL=http://localhost:81.
  cerulean-npm:
    image: jc21/nginx-proxy-manager:latest
    container_name: cerulean-npm
    profiles: ["npm"]
    restart: unless-stopped
    environment:
      PUID: 1000
      PGID: 1000
      TZ: ${TZ:-UTC}
    ports:
      - "80:80"
      - "443:443"
      - "81:81"
    volumes:
      - ./data/npm:/data
      - ./data/npm/letsencrypt:/etc/letsencrypt

  # ── Authentik (OPT-IN: docker compose --profile authentik up -d) ────────
  # SSO / user management. Cerulean authenticates against it via OIDC
  # (AUTHENTIK_ISSUER_URL / AUTHENTIK_CLIENT_ID / AUTHENTIK_CLIENT_SECRET).
  # Reach it at http://<host>:9000, or through the auth.<base> proxy host.
  authentik-server:
    image: ghcr.io/goauthentik/server:${AUTHENTIK_IMAGE_TAG:-2024.12.2}
    container_name: cerulean-authentik
    profiles: ["authentik"]
    restart: unless-stopped
    command: server
    environment:
      # Note: no :? syntax here — compose interpolates env for every service
      # even when the profile is inactive, so these must tolerate being unset.
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY:-}
      AUTHENTIK_ERROR_REPORTING__ENABLED: "false"
      AUTHENTIK_POSTGRESQL__HOST: authentik-postgres
      AUTHENTIK_POSTGRESQL__NAME: authentik
      AUTHENTIK_POSTGRESQL__USER: authentik
      AUTHENTIK_POSTGRESQL__PASSWORD: ${AUTHENTIK_POSTGRESQL_PASSWORD:-}
      AUTHENTIK_REDIS__HOST: authentik-redis
      AUTHENTIK_REDIS__CACHE_TIMEOUT_OVERRIDES__default: "86400"
      AUTHENTIK_BOOTSTRAP_TOKEN: ${AUTHENTIK_BOOTSTRAP_TOKEN:-}
      AUTHENTIK_BOOTSTRAP_PASSWORD: ${AUTHENTIK_BOOTSTRAP_PASSWORD:-}
      AUTHENTIK_BOOTSTRAP_EMAIL: ${AUTHENTIK_BOOTSTRAP_EMAIL:-admin@cerulean.innotel.us}
    ports:
      - "9000:9000"
      - "9443:9443"
    volumes:
      - ./data/authentik/media:/media
      - ./data/authentik/custom-templates:/templates
    depends_on:
      authentik-postgres:
        condition: service_healthy
      authentik-redis:
        condition: service_healthy

  authentik-worker:
    image: ghcr.io/goauthentik/server:${AUTHENTIK_IMAGE_TAG:-2024.12.2}
    container_name: cerulean-authentik-worker
    profiles: ["authentik"]
    restart: unless-stopped
    command: worker
    environment:
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY:-}
      AUTHENTIK_ERROR_REPORTING__ENABLED: "false"
      AUTHENTIK_POSTGRESQL__HOST: authentik-postgres
      AUTHENTIK_POSTGRESQL__NAME: authentik
      AUTHENTIK_POSTGRESQL__USER: authentik
      AUTHENTIK_POSTGRESQL__PASSWORD: ${AUTHENTIK_POSTGRESQL_PASSWORD:-}
      AUTHENTIK_REDIS__HOST: authentik-redis
    volumes:
      - ./data/authentik/media:/media
      - ./data/authentik/custom-templates:/templates
    depends_on:
      authentik-postgres:
        condition: service_healthy
      authentik-redis:
        condition: service_healthy

  authentik-postgres:
    image: docker.io/library/postgres:16-alpine
    container_name: cerulean-authentik-postgres
    profiles: ["authentik"]
    restart: unless-stopped
    environment:
      POSTGRES_DB: authentik
      POSTGRES_USER: authentik
      POSTGRES_PASSWORD: ${AUTHENTIK_POSTGRESQL_PASSWORD:-}
    volumes:
      - ./data/authentik/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U authentik"]
      interval: 5s
      timeout: 5s
      retries: 10

  authentik-redis:
    image: docker.io/library/redis:alpine
    container_name: cerulean-authentik-redis
    profiles: ["authentik"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ── HashiCorp Vault (OPT-IN: docker compose --profile vault up -d) ───────
  # Secret vault for private keys and credentials. This runs Vault in DEV
  # mode (ephemeral, single unsealed node) for a self-hosted stack — for
  # anything beyond testing, point VAULT_ADDR at a real Vault instead.
  vault:
    image: hashicorp/vault:${VAULT_IMAGE_TAG:-1.17}
    container_name: cerulean-vault
    profiles: ["vault"]
    restart: unless-stopped
    command: server -dev -dev-root-token-id=${VAULT_DEV_ROOT_TOKEN_ID:-cerulean-root}
    environment:
      VAULT_DEV_LISTEN_ADDRESS: 0.0.0.0:8200
    ports:
      - "8200:8200"
    cap_add:
      - IPC_LOCK

